public int countXX(String str) {
    // Initialize a counter to track the number of "xx" occurrences
    int count = 0;
    
    // Loop through the string up to the second-to-last character
    for (int i = 0; i < str.length() - 1; i++) {
        // Check if the substring from index i to i+2 is "xx"
        if (str.substring(i, i + 2).equals("xx")) {
            // If "xx" is found, increment the counter
            count++;
        }
    }
    
    // Return the total count of "xx" occurrences found
    return count;
}
