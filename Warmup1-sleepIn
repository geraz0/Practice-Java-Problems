public boolean sleepIn(boolean weekday, boolean vacation) {
    // Check if it is not a weekday or if we are on vacation.
    if (!weekday || vacation) {
        // If either condition is true, we can sleep in.
        return true;
    }
    
    // If it's a weekday and we're not on vacation, we cannot sleep in.
    return false; 
}
